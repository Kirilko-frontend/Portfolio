import styles from './styles.module.scss';

const Hard = () => {
  return (
    <div className={styles['hard']}>
      <h2 className={styles['hard__title']}>Hard</h2>
    </div>
  );
};

export default Hard;
