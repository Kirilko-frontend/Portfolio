import clsx from "clsx";

import styles from "./styles.module.scss";

const Main = () => {
  return (
    <section className={clsx(styles.main)}>
      <h1>Main</h1>
    </section>
  );
};

export default Main;
